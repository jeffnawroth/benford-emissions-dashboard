# Copy to .env.local for local development. Points the frontend at the
# separately-running `wrangler dev` server (apps/worker), since `next dev`
# and `wrangler dev` run on different ports locally.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787
